import { Injectable } from '@angular/core';

@Injectable()
export class NoteService {

  
    
    cancelSubscriptions() {
        // this.fbSubs.forEach(sub => {
        //     if (sub) {
        //         sub.unsubscribe();
        //     }
        // });
    }
}